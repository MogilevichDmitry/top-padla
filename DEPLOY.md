# Деплой Telegram бота

⚠️ **ПЕРЕД ДЕПЛОЕМ**: Убедитесь, что все другие экземпляры бота остановлены (локальные и другие серверы). Telegram не позволяет запускать несколько экземпляров бота одновременно!

## 🆓 Бесплатные варианты деплоя

### ✅ Вариант 1: Railway.app (Рекомендуется - бесплатный)

**Railway предоставляет $5 кредитов бесплатно каждый месяц**, чего более чем достаточно для работы простого бота!

### Вариант 2: Fly.io (Бесплатный тариф)

Fly.io предлагает бесплатный тариф с ограниченными ресурсами, но достаточный для Telegram бота.

### Вариант 3: Koyeb (Бесплатный тариф)

Koyeb предоставляет бесплатный тариф для небольших приложений.

---

## Вариант 1: Railway.app (Рекомендуется - бесплатный)

Railway предоставляет **$5 кредитов бесплатно каждый месяц**, чего более чем достаточно для работы простого бота (примерно $0.01-0.02 в день).

### Шаги:

1. **Создайте аккаунт на [Railway.app](https://railway.app)**

   - Регистрация через GitHub (проще всего)

2. **Создайте новый проект:**

   - Нажмите "New Project"
   - Выберите "Deploy from GitHub repo"
   - Выберите репозиторий `top-padla`

3. **Настройки:**

   - Railway автоматически определит Python проект
   - Использует `Procfile` для запуска (если есть)
   - Или автоматически определит команду запуска

4. **Environment Variables:**
   В настройках проекта (Variables tab) добавьте:

   ```
   BOT_TOKEN=your_telegram_bot_token
   WEB_API_URL=https://qwerty123.eu
   ```

5. **План:**

   - По умолчанию используется Starter план ($5/месяц)
   - **Но первые $5 каждый месяц бесплатны!**
   - Для простого бота этого хватит (примерно $0.01-0.02 в день)

6. **Деплой:**

   - Railway автоматически задеплоит
   - Проверьте логи в Dashboard → ваш проект → Logs

7. **Проверка:**
   - Бот должен появиться онлайн в Telegram
   - Попробуйте команду `/start` в боте
   - Проверьте логи на ошибки

---

## Вариант 2: Fly.io (Бесплатный тариф)

Fly.io предлагает бесплатный тариф, достаточный для Telegram бота.

### Шаги:

1. **Установите Fly CLI:**

   ```bash
   curl -L https://fly.io/install.sh | sh
   ```

2. **Создайте аккаунт:**

   ```bash
   fly auth signup
   ```

3. **Деплой:**

   ```bash
   cd /Users/dmitrymogilevich/projects/top-padla
   fly launch
   ```

   - Ответьте на вопросы
   - Выберите регион (например, `fra` для Франкфурта)

4. **Настройте переменные окружения:**

   ```bash
   fly secrets set BOT_TOKEN=your_telegram_bot_token
   fly secrets set WEB_API_URL=https://qwerty123.eu
   ```

5. **Задеплойте:**

   ```bash
   fly deploy
   ```

6. **Проверьте логи:**
   ```bash
   fly logs
   ```

Файл `fly.toml` уже готов в репозитории.

---

## Вариант 3: Koyeb (Бесплатный тариф)

Koyeb предоставляет бесплатный тариф для небольших приложений.

### Шаги:

1. **Создайте аккаунт на [Koyeb.com](https://www.koyeb.com)**

2. **Создайте новый App:**

   - Нажмите "Create App"
   - Подключите GitHub репозиторий `top-padla`

3. **Настройки:**

   - Type: `Web Service` (Koyeb поддерживает долгоживущие процессы)
   - Build Command: `pip install -r requirements.txt`
   - Run Command: `python main.py`

4. **Environment Variables:**

   ```
   BOT_TOKEN=your_telegram_bot_token
   WEB_API_URL=https://qwerty123.eu
   ```

5. **Деплой:**
   - Нажмите "Deploy"
   - Koyeb автоматически задеплоит бота

---

## Вариант 4: Переписать на Webhook для Vercel (Полностью бесплатно)

Если нужно использовать именно Vercel, можно переписать бота на webhook. Это потребует:

1. Создать API route `/api/webhook` в Vercel
2. Использовать `aiogram.webhook` вместо `aiogram.polling`
3. Настроить webhook URL в Telegram через BotFather

**Преимущества webhook:**

- ✅ Полностью бесплатно на Vercel
- ✅ Меньше нагрузка (обновления приходят от Telegram)
- ✅ Работает на Vercel serverless functions

**Недостатки:**

- Требует переписывания кода
- Нужен публичный URL для webhook

**Если хотите этот вариант - могу помочь переписать.**

---

## Рекомендация

**Используйте Railway.app** - самый простой и надежный вариант:

- ✅ **Бесплатно** ($5 кредитов каждый месяц)
- ✅ Поддерживает долгоживущие процессы
- ✅ Простой деплой через GitHub
- ✅ Не требует изменений в коде
- ✅ Автоматический рестарт при ошибках
- ✅ Удобный Dashboard для мониторинга

Для простого Telegram бота $5 кредитов в месяц более чем достаточно (примерно $0.01-0.02 в день).
