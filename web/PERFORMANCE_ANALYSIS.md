# Анализ производительности при 1000 матчах

## 📊 Текущая сложность алгоритмов

### 1. `calculateRatings()` - Расчет рейтингов

**Сложность**: O(n log n + m)

- `n` = все матчи в БД
- `m` = матчи в окне 182 дня (обычно меньше n)

**Что происходит**:

1. Сортировка всех матчей по дате: **O(n log n)**
2. Проход по матчам в окне 182 дня: **O(m)**
3. Для каждого матча: простые арифметические операции (O(1))

**При 1000 матчах**:

- Сортировка: ~10,000 операций (1000 \* log₂(1000) ≈ 10,000)
- Проход по матчам: ~1000 операций (если все в окне)
- **Итого**: ~11,000 операций
- **Время**: ~10-50ms (зависит от сервера)

### 2. `getPlayerStats()` - Статистика игрока

**Сложность**: O(n)

- `n` = все матчи в БД

**Что происходит**:

1. Проход по всем матчам: **O(n)**
2. Для каждого матча: проверка участия игрока, подсчет статистики

**При 1000 матчах и 10 игроках**:

- Для одного игрока: ~1000 итераций
- Для всех игроков (в `/api/ratings`): 10 \* 1000 = **10,000 итераций**
- **Время**: ~20-100ms

### 3. `getLeagueRecords()` - Рекорды лиги

**Сложность**: O(n + n\*m)

- `n` = все матчи
- `m` = количество игроков

**Что происходит**:

1. Симуляция всех матчей для поиска пиков/минимумов: **O(n)**
2. Для каждого игрока вызывает `getPlayerStats()`: **O(n\*m)**

**При 1000 матчах и 10 игроках**:

- Симуляция матчей: ~1000 итераций
- Статистика для всех игроков: 10 \* 1000 = 10,000 итераций
- **Итого**: ~11,000 операций
- **Время**: ~50-200ms

### 4. `/api/players/[slug]` - Детальная страница игрока

**Сложность**: O(n + n + n + n) = O(n)

- Вызывает `calculateRatings()`: O(n log n)
- Вызывает `getPlayerStats()`: O(n)
- Вызывает `getRatingHistory()`: O(n) - симулирует историю
- Вызывает `getPerformanceByOpponentStrength()`: O(n)
- Вызывает `getPlayerStreaks()`: O(n)

**При 1000 матчах**:

- **Время**: ~100-500ms

---

## ⚠️ Проблемы при масштабировании

### При 1000 матчах

#### ✅ Еще не критично, но заметно:

- **Standings page** (`/api/ratings`): ~50-150ms
- **Players page** (`/api/players/stats`): ~50-150ms
- **Player profile** (`/api/players/[slug]`): ~100-500ms
- **Records page** (`/api/records`): ~100-300ms

**Итого на страницу**: ~50-500ms (приемлемо, но не идеально)

#### ⚠️ Проблемы:

1. **Каждый запрос пересчитывает все с нуля**

   - Нет кеширования на сервере
   - Даже одинаковые запросы выполняют полный пересчет

2. **Дублирование вычислений**

   - `/api/ratings` и `/api/players/stats` делают одно и то же
   - Каждый API route независимо пересчитывает рейтинги

3. **Множественные вызовы `getPlayerStats()`**
   - В `/api/ratings`: вызывается для каждого игрока (10 раз)
   - В `/api/records`: вызывается для каждого игрока (10 раз)
   - **Итого**: 20 \* 1000 = 20,000 лишних итераций

### При 5000 матчах

#### ❌ Станет проблемой:

- **Standings page**: ~250-750ms (медленно)
- **Player profile**: ~500-2500ms (очень медленно)
- **Records page**: ~500-1500ms (медленно)

**Проблемы усугубляются**:

- Сортировка матчей: O(n log n) растет быстрее
- Множественные проходы по матчам становятся дорогими
- Без кеширования каждый запрос будет медленным

### При 10000+ матчах

#### ❌ Критическая проблема:

- **Время ответа**: 1-5+ секунд
- **Нагрузка на сервер**: высокая
- **UX**: плохой (долгие загрузки)

---

## 🔍 Детальный анализ операций

### Пример: `/api/ratings` при 1000 матчах и 10 игроках

```
1. Загрузка из БД:
   - SELECT * FROM players: ~10 записей, ~1ms
   - SELECT * FROM matches: ~1000 записей, ~5-10ms

2. calculateRatings():
   - Сортировка: 1000 * log₂(1000) ≈ 10,000 операций
   - Проход по матчам: ~1000 итераций
   - Время: ~10-50ms

3. getPlayerStats() для каждого игрока (10 раз):
   - 10 * 1000 = 10,000 итераций
   - Время: ~20-100ms

4. Сортировка результатов: ~10 * log₂(10) ≈ 33 операции
   - Время: <1ms

Итого: ~35-160ms
```

### Пример: `/api/players/[slug]` при 1000 матчах

```
1. Загрузка из БД: ~5-10ms

2. calculateRatings(): ~10-50ms

3. getPlayerStats(): ~1-5ms (только для одного игрока)

4. getRatingHistory():
   - Симулирует историю для каждого матча игрока
   - Если игрок сыграл 200 матчей: ~200 итераций
   - Время: ~10-50ms

5. getPerformanceByOpponentStrength():
   - Проходит по всем матчам игрока: ~200 итераций
   - Время: ~5-20ms

6. getPlayerStreaks(): ~1-5ms

7. Вычисление партнеров: ~5-20ms

Итого: ~36-160ms (для активного игрока)
```

---

## ✅ Решения (из плана оптимизации)

### 1. API Route Caching (быстрое решение)

**Эффект**: Снижает время ответа в 10-100 раз для повторных запросов

- Первый запрос: ~50-500ms
- Повторные запросы (в течение 60 сек): ~1-5ms (из кеша)

### 2. Pre-computed Statistics (долгосрочное решение)

**Эффект**: Снижает время ответа в 100-1000 раз

- Вместо пересчета: просто SELECT из БД
- Время: ~1-5ms (вместо 50-500ms)

### 3. Database Indexes

**Эффект**: Ускоряет SQL запросы в 10-100 раз

- SELECT \* FROM matches: ~5-10ms → ~0.5-1ms

### 4. Incremental Updates

**Эффект**: При добавлении матча пересчитывается только измененное

- Вместо пересчета всех 1000 матчей: пересчет только нового матча
- Время добавления: ~1-5ms (вместо 50-500ms)

---

## 📈 Прогноз производительности

| Матчей | Текущее    | С кешированием | С pre-computed |
| ------ | ---------- | -------------- | -------------- |
| 100    | 10-50ms    | 1-5ms (кеш)    | 1-2ms          |
| 1000   | 50-500ms   | 1-5ms (кеш)    | 1-3ms          |
| 5000   | 250-2500ms | 1-5ms (кеш)    | 1-5ms          |
| 10000  | 500-5000ms | 1-5ms (кеш)    | 2-10ms         |

---

## 🎯 Вывод

### При 1000 матчах:

- ✅ **Еще работает**, но уже заметны задержки
- ⚠️ **Рекомендуется** добавить кеширование
- ⚠️ **Желательно** начать планировать pre-computed statistics

### При 5000+ матчах:

- ❌ **Будет проблемой** без оптимизации
- ✅ **Обязательно** нужно кеширование
- ✅ **Критично** нужны pre-computed statistics

### Рекомендация:

**Начать оптимизацию сейчас**, пока данных еще немного:

1. API Route Caching (быстро, большой эффект)
2. Database Indexes (быстро, средний эффект)
3. Pre-computed Statistics (средний приоритет, но критично для масштабирования)
