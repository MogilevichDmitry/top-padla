# Архитектура данных и вычислений

## 📦 Что хранится в БД (Raw Data)

### Таблица `players`
```sql
- id (INTEGER PRIMARY KEY)
- name (TEXT)
- tg_id (INTEGER, nullable)
```
**Хранится**: Только базовая информация об игроках

### Таблица `matches`
```sql
- id (INTEGER PRIMARY KEY)
- date (TIMESTAMP)
- type (TEXT: "to6" | "to4" | "to3")
- team_a (INTEGER[]) - массив ID игроков
- team_b (INTEGER[]) - массив ID игроков
- score_a (INTEGER)
- score_b (INTEGER)
- created_by (INTEGER, nullable)
```
**Хранится**: Только результаты матчей (сырые данные)

### Таблица `pairs`
```sql
- id (INTEGER PRIMARY KEY)
- player1_id (INTEGER)
- player2_id (INTEGER)
- rating (DECIMAL)
- matches (INTEGER)
- wins (INTEGER)
- losses (INTEGER)
```
**Хранится**: Предвычисленные рейтинги пар (обновляются отдельно)

---

## ⚙️ Что вычисляется на СЕРВЕРЕ (API Routes)

### Все вычисления происходят в API routes на сервере Next.js

#### `/api/ratings` → `calculateRatings()` + `getPlayerStats()`
**Что делает**:
1. Загружает всех игроков и матчи из БД
2. **Вычисляет рейтинги** - проходит по всем матчам за последние 182 дня и пересчитывает рейтинги
3. **Вычисляет статистику** для каждого игрока (матчи, победы, поражения, win rate)
4. Возвращает готовые данные

#### `/api/players/stats` → `calculateRatings()` + `getPlayerStats()`
**Что делает**: То же самое, но для всех игроков (включая неактивных)

#### `/api/players/[slug]` → Множество вычислений
**Что делает**:
1. `calculateRatings()` - текущие рейтинги
2. `getPlayerStats()` - базовая статистика
3. `getRatingHistory()` - история рейтинга (симуляция по времени)
4. `getPerformanceByOpponentStrength()` - производительность против сильных/слабых
5. `getPlayerStreaks()` - стрики побед/поражений
6. Вычисление партнеров (лучшие/худшие)
7. Вычисление ранга игрока

#### `/api/records` → `getLeagueRecords()`
**Что делает**:
1. Загружает всех игроков и матчи
2. **Симулирует все матчи** для поиска рекордов
3. Находит максимальные/минимальные рейтинги
4. Находит лучшие/худшие дуэты
5. Находит самые длинные стрики

#### `/api/pairs`
**Что делает**: Просто загружает предвычисленные данные из таблицы `pairs`

---

## 💻 Что происходит на КЛИЕНТЕ

### Клиент НЕ вычисляет ничего!

**Клиент только**:
1. ✅ Отправляет запросы к API routes
2. ✅ Получает готовые данные (JSON)
3. ✅ Кеширует данные через React Query
4. ✅ Отображает данные в UI
5. ✅ Сортирует/фильтрует уже готовые данные

**Клиент НЕ**:
- ❌ Не вычисляет рейтинги
- ❌ Не обращается к БД напрямую
- ❌ Не выполняет тяжелые вычисления

---

## 🔄 Поток данных

```
┌─────────────┐
│   БД (Postgres)  │
│  - players       │
│  - matches       │
│  - pairs         │
└──────┬──────┘
       │
       │ SQL запросы
       ▼
┌─────────────────────┐
│  API Routes (Server)│
│  - /api/ratings     │
│  - /api/players/*   │
│  - /api/records     │
│                     │
│  Вычисления:        │
│  - calculateRatings()│
│  - getPlayerStats() │
│  - getLeagueRecords()│
└──────┬──────────────┘
       │
       │ JSON ответ
       ▼
┌─────────────────────┐
│  React Query (Client)│
│  - Кеширование       │
│  - Дедупликация      │
└──────┬──────────────┘
       │
       │ Готовые данные
       ▼
┌─────────────────────┐
│  React Components    │
│  - Отображение       │
│  - Сортировка        │
└─────────────────────┘
```

---

## ⚠️ Проблемы текущей архитектуры

### 1. Все вычисляется на лету
- **Каждый запрос** → загружает всех игроков и все матчи
- **Каждый запрос** → пересчитывает рейтинги с нуля
- **Каждый запрос** → пересчитывает всю статистику

### 2. Нет кеширования на сервере
- Даже одинаковые запросы выполняют полный пересчет
- Нет кеширования результатов вычислений

### 3. Дублирование вычислений
- `/api/ratings` и `/api/players/stats` делают одно и то же
- Каждый API route пересчитывает рейтинги независимо

### 4. Таблица `pairs` не используется везде
- Рейтинги пар хранятся в БД, но все равно пересчитываются
- Не синхронизированы с текущими рейтингами игроков

---

## ✅ Что уже оптимизировано

1. **React Query на клиенте**
   - Кеширование на 5 минут
   - Дедупликация запросов
   - Быстрая навигация между страницами

2. **Параллельные запросы**
   - `Promise.all()` для загрузки players и matches одновременно

---

## 🚀 Что можно улучшить (из плана оптимизации)

1. **API Route Caching** - кешировать результаты на сервере
2. **Pre-computed Statistics** - хранить вычисленные данные в БД
3. **Database Indexes** - ускорить SQL запросы
4. **Incremental Updates** - пересчитывать только измененные данные

---

## 📊 Пример: Что происходит при открытии страницы Standings

1. **Клиент**: `useRatings()` → запрос к `/api/ratings`
2. **Сервер**: 
   - Загружает всех игроков из БД (SQL: `SELECT * FROM players`)
   - Загружает все матчи из БД (SQL: `SELECT * FROM matches`)
   - Вызывает `calculateRatings()` - проходит по всем матчам за 182 дня
   - Для каждого игрока вызывает `getPlayerStats()` - проходит по всем матчам
   - Сортирует результаты
3. **Сервер**: Возвращает JSON с готовыми данными
4. **Клиент**: React Query кеширует данные
5. **Клиент**: Компонент отображает данные

**Время**: ~500-1000ms (зависит от количества матчей)

**При повторном открытии** (в течение 5 минут):
- Данные берутся из кеша React Query
- **Время**: ~0ms (мгновенно)

